# Optional deps for local /adv-protect (PhotoGuard Encoder).
# Do NOT merge into the production Docker image.
#
# Install (CPU example):
#   pip install -r requirements-adv.txt
# GPU: install a matching torch build from https://pytorch.org first, then:
#   pip install -r requirements-adv.txt

torch>=2.1,<3
diffusers>=0.27,<0.32
transformers>=4.38,<5
accelerate>=0.27,<1
safetensors>=0.4,<1
huggingface-hub>=0.20,<1
